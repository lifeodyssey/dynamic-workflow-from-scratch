export const meta = { name: 'smoke', description: 'one-agent liveness check for the real backend' }

const reply = await agent('Reply with exactly this phrase and nothing else: dynamic workflow online')
return reply
